layout ipv4
{
	type : int;
}

layout eth
{
	src : int;
	dst : int;
	type : int;
	ip : ipv4;
}

decoder eth_d(eth)
{
	extract eth::src;
	extract eth::ip;
	extract eth::ip::type;

	eth::src;
	eth::ip;
	eth::ip::type;
}