layout eth
{
  dst  : uint(48);
  src  : uint(48);
  vlan_tag : vlan;
  type : uint(16);
}

layout vlan
{
  type : uint(16);
  tci  : uint(16);
}

decoder start eth_d(eth)
{
  extract eth.vlan_tag.type;
  
  drop;
}
