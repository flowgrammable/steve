Port p1 = ":5000";

layout eth
{
	src : uint(48);
	dst : uint(48);
	type : uint(16);
}

decoder start eth_d(eth)
{
	extract eth::dst;
	extract eth::src;
  extract eth::type;
	// set eth::src = eth::dst & 0xff_ff_ff_ff_ff_00;
  // set eth::type = 800;
	// clear;
  // copy eth::type = eth::src;
  // write drop;
  // write output p1;
  write set eth::type = 0x800;
  // write copy eth::type = eth::src;
}
