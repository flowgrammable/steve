foreign def puts(char[]) -> int;

layout eth
{
	src : uint(48);
	dst : uint(48);
	type : uint(16);
}


decoder start eth_d(eth)
{
	extract eth.src;
  extract eth.dst;
  goto learn;
}


event switch_learn(eth.src)
{
  add
  { eth.src } ->
  {
    drop;
  }
  into switch;

  goto switch;
}


exact_table learn(eth.src)
{
  miss ->
  {
    raise switch_learn;
		goto switch;
  }
}


exact_table switch(eth.dst)
{
  miss ->
  {
    flood;
  }
}
