layout eth
{
	src : int;
	dst : int;
	type : int;
}

decoder start eth_d(eth)
{
	extract eth::src;
	extract eth::dst;
	goto t1;
}

exact_table t1(eth::src, eth::dst)
{
	{0, "string"} -> { drop; }
	{3, 4} -> { drop; }
}
